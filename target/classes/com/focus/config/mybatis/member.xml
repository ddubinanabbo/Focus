<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.focus.member.dao.MemberDao">
    
    <select id="idcheck" parameterType="String" resultType="int">
        select count(M_ID)
        from member 
        where M_ID = #{M_ID}
    </select>
    
      <insert id="registerMember" parameterType="com.focus.member.model.MemberDto">
        insert into member (M_ID, M_PASS, M_NAME, M_JOB, M_AGE, M_EMAIL, M_TEL, M_JOINDATE)
        values(#{M_ID}, #{M_PASS}, #{M_NAME}, #{M_JOB}, #{M_AGE},#{M_EMAIL}, #{M_TEL}, now()) 
        
    </insert>
    
    <select id="login" parameterType="java.util.Map" resultType="com.focus.member.model.MemberDto">
        select M_ID, M_NAME
        from member
        where M_ID = #{userid} and M_PASS = #{userpwd}
    </select>
    
    
    
    
</mapper>
